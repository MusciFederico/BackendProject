<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ name }}</title>
    <style>
        /* Estilos para el navbar */
        nav {
            background-color: #333;
            color: #fff;
            padding: 10px 0;
            text-align: center;
        }

        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            display: inline;
            margin-right: 10px;
        }

        nav ul li a {
            color: #fff;
            text-decoration: none;
        }

        a {
            text-decoration: none;
            color: inherit;
            cursor: default;
        }
    </style>
</head>

<body>
    <nav>
        <ul>
            {{#if role0}}
            <li><a href="/">Home</a></li>
            <li><a href="/cart">Cart</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><button id="signout" type="submit">Sign out</button></li>
            {{/if}}
            {{#if role1}}
            <li><a href="/">Home</a></li>
            <li><a href="/products/form">Form</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><button id="signout">Sign out</button></li>
            {{/if}}
            {{#if role2}}
            <li><a href="/">Home</a></li>
            <li><a href="/products/form">Form</a></li>
            <li><a href="/cart">cart</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><a href="/products/me">My products</a></li>
            <li><button id="signout">Sign out</button></li>
            {{/if}}
            {{#if roleUnd}}
            <li><a href="/">Home</a></li>
            <li><a href="/auth/login">Login</a></li>
            <li><a href="/auth/register">Register</a></li>
            {{/if}}
        </ul>
    </nav>

    <h1>{{ name }}</h1>

    <ul>
        <li>
            <p>Photo: <img src="{{profile.photo}}" alt="User Photo" width="100" height="100"></p>

            <p>Name: {{profile.name}}</p>
            <p>Email: {{profile.email}}</p>
            <p>Age: {{profile.age}}</p>
            <p>Verified: {{profile.verified}}</p>
            {{#if role0}}
            <p>Role: User</p>
            {{/if}}
            {{#if role1}}
            <p>Role: Admin</p>
            {{/if}}
            {{#if role2}}
            <p>Role: Premium</p>
            {{/if}}
            {{#if roleUnd}}
            <p>Role: you are not suposed to see this</p>
            {{/if}}
            <p>Creation date: {{profile.createdAt}}</p>
            <p>Last update: {{profile.updatedAt}}</p>
        </li>
    </ul>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.2.0/socket.io.js"></script>
    <script src="/scripts/sortAndFilter.js"></script>
    <script src="/scripts/cart.js"></script>
    <script src="/scripts/signout.js"></script>
</body>

</html>