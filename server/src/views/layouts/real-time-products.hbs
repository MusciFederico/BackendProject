<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>

<body>

    <h1>{{ title }}</h1>

    <ul id="products-list">
        {{#each products}}
        <li>{{ this.name }} - Price: ${{ this.price }}</li>
        {{/each}}
    </ul>

    <!-- No need for Socket.IO in this case -->

</body>

</html>